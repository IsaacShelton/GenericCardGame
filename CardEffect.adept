
import 'main.adept'

struct CardEffect (identifier String, value String)

func clone(this *CardEffect) CardEffect {
    effect POD CardEffect
    effect.identifier = this.identifier.clone()
    effect.value = this.value.clone()
    return effect
}

func clone(this *<CardEffect> List) <CardEffect> List {
    list <CardEffect> List
    each CardEffect in *this, list.add(it.clone())
    return list.commit()
}
